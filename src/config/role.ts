export const EMPLOYEE = 'Employee'
export const HR = 'HR Manager'
export const GUEST = 'Guest'

export const isEmployee = (role: string) => role === EMPLOYEE
export const isHR = (role: string) => role === HR
export const isGuest = (role: string) => role === GUEST
